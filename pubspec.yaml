name: mobile_web_firebase
description: Simple Tutorial for Firebase Authentication (login) and Firestore on Flutter mobile and web
version: 1.0.0+1
environment:
  sdk: ">=2.1.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3


#  Flutter Mobile Dependencies
  firebase_core: ^0.4.2+1
  cloud_firestore: ^0.12.11
  firebase_auth: ^0.15.1
  google_sign_in: ^4.0.14

#Comment out these dependencies when running mobile version
#  Flutter Web Dependencies
  firebase: 7.1.0
  firebase_web: 5.0.9